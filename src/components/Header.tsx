import Navbar from './Navbar';

const Header = () => {
  return (
    <header className="fixed w-full z-50 md:fixed md:-mx-20 md:mt-12 lg:static lg:mx-0">
      <Navbar />
    </header>
  );
};

export default Header;
